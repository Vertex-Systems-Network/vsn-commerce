import {useEffect} from 'react';
/** Handles seo for the VSN Ecommerce interface. */
export default function SEO({title='VSN Ecommerce',description='Shop verified products, deals and rewards on VSN Ecommerce.',canonical}){useEffect(/** Inline callback for this operation. */ ()=>{document.title=title;const set=/** Handles set for the VSN Ecommerce interface. */ (sel,attr,val)=>{let el=document.querySelector(sel);if(!el){el=document.createElement('meta');document.head.appendChild(el)}el.setAttribute(attr[0],attr[1]);el.setAttribute('content',val)};set('meta[name="description"]',['name','description'],description);set('meta[property="og:title"]',['property','og:title'],title);set('meta[property="og:description"]',['property','og:description'],description);if(canonical){let l=document.querySelector('link[rel="canonical"]');if(!l){l=document.createElement('link');l.rel='canonical';document.head.appendChild(l)}l.href=canonical}},[title,description,canonical]);return null}
